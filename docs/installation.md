# Installation Guide

## Requirements
- GTMOS core environment framework.
- YAML parser + JSON Schema validation parser library.

## Integration Steps
1. Place the `growth-experiment/` directory into your GTMOS active skills directory path.
2. Register the schemas under the `/schemas` directory with your pipeline schema registry.
3. Configure the agent context path to import system prompts from the `/prompts` folder.
